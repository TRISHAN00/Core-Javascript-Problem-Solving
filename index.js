// random number
function randomNumber(n) {
  return Math.floor(Math.random() * n + 1);
}

// sorting names
const names = [
  "Zoe Wang",
  "Darius Mclean",
  "Valentin Ortiz",
  "Declan Krueger",
  "Annabella Webster",
];

function sortingName(arr) {
  return arr.sort();
}
